enum ContentType {
  json,
  formUrlEncoded,
  textPlain,
}

enum PlaceholderKey {subject, description, username, password}

enum NetworkCallStatus {loading, success, fail, error}

enum AppKeys {authToken,}